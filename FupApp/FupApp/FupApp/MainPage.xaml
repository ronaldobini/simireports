<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:FupApp"
             x:Class="FupApp.MainPage">

    <NavigationPage.TitleView>
        <StackLayout Orientation="Horizontal" Spacing="10">
            <!--<Image Source="imgs/angryasf.png" HorizontalOptions="Center">
            </Image>-->
            <Label Text="SimiApp" FontSize="20" TextColor="White" HorizontalTextAlignment="Center"/>
        </StackLayout>
    </NavigationPage.TitleView>

    <ScrollView>
        <ListView x:Name="listView">

            <ListView.ItemTemplate >
                <DataTemplate>
                    <!--<TextCell Text="{Binding}" TextColor="#399bfc"/>-->
                    <ViewCell>
                        <StackLayout VerticalOptions="FillAndExpand" HorizontalOptions="Center">
                            <Label Text="{Binding}" Font="30" TextColor="#399bfc" />
                        </StackLayout>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>

        </ListView>

    </ScrollView>
</ContentPage>
